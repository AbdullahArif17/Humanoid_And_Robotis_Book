// Configuration file for the Humanoid & Robotics Book frontend
// This allows us to manage configuration values in one place

// The BACKEND_URL defaults to http://localhost:8000 for local development
const config = {
  BACKEND_URL: 'http://localhost:8000',
};

export default config;